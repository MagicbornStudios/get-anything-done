# gad-requirements-gui-bare — requirements

See [`evals/gad-requirements-gui/REQUIREMENTS.md`](../gad-requirements-gui/REQUIREMENTS.md) for the full task specification. This project uses the same requirements and rubric; only the workflow differs (`bare` instead of `gad`).
