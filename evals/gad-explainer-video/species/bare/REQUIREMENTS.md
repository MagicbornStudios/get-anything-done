# gad-explainer-video-bare — requirements

See [`evals/gad-explainer-video/REQUIREMENTS.md`](../gad-explainer-video/REQUIREMENTS.md) for the full task specification. This project uses the same requirements and rubric; only the workflow differs (`bare` instead of `gad`).
