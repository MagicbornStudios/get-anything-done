# Optional: required only for `gad evolution images generate`
# Leave unset if you do not use image generation.
OPENAI_API_KEY=

# Optional model override in your shell when needed.
# OPENAI_MODEL=gpt-image-1
