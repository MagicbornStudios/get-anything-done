# {{slug}}

> Generated by `gad site new {{slug}}`.

## Purpose

<!-- One-paragraph description of what this site does and who it serves. -->

## Stack

- Next.js 16 + React 19
- Tailwind CSS v4 (CSS-only config, no tailwind.config.js)
- GAD Visual Context System (`gad-visual-context`)
- `@gad/ui` workspace component library
- Vercel deployment

## Key links

- Vercel project: <!-- paste dashboard link -->
- Domain: <!-- e.g. https://{{slug}}.example.com -->
- GAD project id: `{{slug}}`

## Development

```sh
cd sites/{{slug}}
pnpm dev          # http://localhost:3000
pnpm build        # production build
pnpm typecheck    # tsc --noEmit
```
