---
status: candidate
source_phase: "68"
source_phase_title: "Monorepo reorg — rename planning-app → platform, move portfolio-v2, merge marketing"
pressure_score: 11.67
tasks_total: 3
tasks_done: 3
crosscuts: 1
created_on: "2026-04-23"
created_by: compute-self-eval
---

# Candidate from phase 68

## Phase

```
get-anything-done | 68 | Monorepo reorg — rename planning-app → platform, move portfolio-v2, merge marketing
selection pressure: 11.67  (3 tasks, 3 done, 1 crosscuts)
```

## Tasks

```
68-01 done Rename apps/planning-app → apps/platform. Mechanical sweep. (1) git mv apps/planning-app apps/platform. (2) Update apps/platform/package.json name: @portfolio/planning-app → @portfolio/platform. (3) grep -rl &apos;@portfolio/planning-app&apos; across monorepo and replace with @portfolio/platform — touch apps/*, packages/*, vendor/*, root package.json, turbo/pnpm configs. (4) Update root package.json scripts gad:planning:* → gad:platform:* (OR keep old names as aliases for muscle memory — decide in-task). (5) Verify: pnpm install clean, pnpm --filter @portfolio/platform typecheck passes, pnpm --filter @portfolio/platform build passes if feasible without starting Next dev.
68-02 done Move apps/portfolio-v2 → apps/species/portfolio-v2. Operator directive: portfolio-v2 is a species being dogfooded (phase 50), not a top-level app. (1) mkdir apps/species if missing. (2) git mv apps/portfolio-v2 apps/species/portfolio-v2. (3) Update root pnpm-workspaces config to include apps/species/* if not already wildcarded. (4) grep -rl &apos;@portfolio/v2&apos; or &apos;apps/portfolio-v2&apos; across monorepo and fix paths/package refs. (5) Update root package.json scripts dev:portfolio-v2, build:portfolio-v2 to filter-pattern that still matches @portfolio/v2 (since package name stays the same, most things should just work). (6) Verify pnpm install clean + typecheck.
68-03 done Copy vendor/get-anything-done/site/ marketing shell into apps/platform/app/(marketing)/ as the platform&apos;s public marketing surface. Operator decision this session (overrides gad-261): single Next app serves unauth marketing (landing, marketplace browse, articles) + authed operator surfaces (/project-editor, /my-published, /byok). (1) Copy landing home + hero + about + pricing (if exists) surfaces into apps/platform/app/(marketing)/*. (2) Reuse @portfolio/visual-context for SiteSection wrappers (already extracted in 53-11). (3) Middleware: marketing routes public, /platform/* authed. (4) Do NOT delete vendor/get-anything-done/site/ — it stays as the GAD framework&apos;s dev/landing site (different audience: framework contributors vs consumers).
```

## What this candidate is for

This file was auto-generated by `compute-self-eval.mjs` because phase
68 exceeded the selection pressure threshold. High pressure
signals a recurring pattern that may want to become a reusable skill.

This is the **raw input** to the drafting step. The drafter (`create-proto-skill`,
invoked by `gad-evolution-evolve`) reads this file and decides what the
skill should be. No curator pre-digestion happens — see the 2026-04-13
evolution-loop experiment finding for why
(`evals/FINDINGS-2026-04-13-evolution-loop-experiment.md`).

## How the drafter should enrich this

The drafter should pull additional context from:

- `gad decisions --projectid get-anything-done | grep -i <keyword>` —
  relevant decisions for this phase
- `git log --follow --oneline <file>` — historical context for files this
  phase touched (catches the "three attempts at task X failed" thread that
  lives in commit history)
- `gad --help` and `gad <subcommand> --help` — CLI surface available
  to the skill
- `ls vendor/get-anything-done/skills/` — existing skills to avoid
  duplicating

The drafter does **not** ask a human for anything. Make decisions and
document them in the SKILL.md.

## Output location

The drafter writes to `.planning/proto-skills/phase-68-monorepo-reorg-rename-planning-app-platf/SKILL.md` — a
**different directory** from this candidate. Candidates and proto-skills
are two distinct stages:

- **candidate** (this file) = raw selection-pressure output
- **proto-skill** (drafter output) = drafted SKILL.md awaiting human review
- **skill** (post-promote) = lives in `skills/` as part of species DNA

The human reviewer runs `gad evolution promote <slug>` or
`gad evolution discard <slug>` after reviewing the proto-skill.
