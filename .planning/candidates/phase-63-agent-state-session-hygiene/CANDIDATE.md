---
status: candidate
source_phase: "63"
source_phase_title: "Agent/state/session hygiene"
pressure_score: 219
tasks_total: 55
tasks_done: 10
crosscuts: 41
created_on: "2026-04-23"
created_by: compute-self-eval
---

# Candidate from phase 63

## Phase

```
get-anything-done | 63 | Agent/state/session hygiene
selection pressure: 219  (55 tasks, 10 done, 41 crosscuts)
```

## Tasks

```
63-01 done State+log consolidation audit. Enumerate every write-path: .omx/state/*.json (current-task-baseline, hud-state, notify-hook-state, skill-active-state, team-state, tmux-hook-state), .planning/{STATE.xml, TASK-REGISTRY.xml, .gad-log/*.jsonl, .trace-events.jsonl, .trace-seq, .gad-agent-lanes.json, graph.json, config.json, sessions/*.json, notes/, todos/}, .claude/{settings.json, settings.local.json, hooks/state}, ~/.cache/gad/. Classify per pattern: (a) contested AGGREGATE → CLI-mediated writes, (b) shared APPEND-ONLY streams → JSONL append-safe, (c) contested SINGLE-OWNER → per-agent dir, (d) cross-agent INTENT → per-file dir + fs.rename. Deliverable: references/state-writes-inventory.md. Follow-ups filed per consolidation action. See 2026-04-18 post-mortem notes.
63-02 done Session continuity contract — &apos;lets continue&apos; zero-cost pickup. Prereq 60-09 handoff queue. (a) Claude session pause writes handoff file, (b) user settings 63-03 stores last-active-projectid, (c) &apos;gad start&apos; or new session auto-invokes gad handoffs list --unclaimed --mine-first + gad snapshot --projectid &lt;last-active&gt;, (d) if exactly one unclaimed handoff matches runtime auto-claim, (e) SessionStart hook surfaces &apos;1 unclaimed handoff — /pickup to resume&apos;. Metrics to beat: 2026-04-18 baseline ~35% of Claude turn on orientation (archaeology + /gsd:update + projectid guessing). Target &lt;1 tool call to restored context. Planning-app side shipped: /pickup page (last-active + mine-first scored handoffs + one-click claim+resume), AppShell PickupHud (header chip with open count), inline claim/complete buttons on /handoffs, last-active stamp on /projects/edit/[id] + /project-editor/[project] visits, vendor bridges for user-settings.cjs + handoffs.cjs (claim/complete via canonical writer, no drift). CLI side (parts c/d/e) remains framework lane.
63-03 done User settings scaffold (non-checked-in). Local ~/.claude/gad-user.json (or .planning/.gad-user.json per-repo, decide per 63-01 audit). Fields: displayName (default = os.userInfo().username), lastActiveProjectid, assignedSouls {projectid: soulslug}, lastSessionId, preferredRuntime, teachingSnoozeUntil. Gitignored. Read by gad startup, gad snapshot, SessionStart hook, and anywhere we need to know &apos;who is logged in&apos;. Addresses 2026-04-18 user feedback: &apos;we should have user settings that dont get checked in so we know who is logged in. if they dont have a name, default to machine name. users can have souls assigned to them in the gad project for that to be injected.&apos;
63-04 done Agent lane manifest — canonical file/directory ownership per agent runtime. Authors a top-level AGENT-LANES.md (or references/agent-lanes.md) that every parallel agent consults as ground truth. Minimum content: (a) per-runtime lane definition (claude-code, codex, cursor, etc.) with file-glob allow + deny lists, (b) shared-write protocols (TASK-REGISTRY.xml via gad tasks add ONLY, etc.), (c) conflict-resolution playbook (who wins on collision, how to cede, how to request cross-lane work), (d) session-start etiquette (read this file BEFORE touching anything). Driven by 2026-04-18 3-agent overlap post-mortem where Cursor misattributed Claude&apos;s tasks to Codex, costing ~5% of its turn. Codex owns runtime/bin/gad.cjs + lib/runtime-* + verify; Cursor owns BYOK/lib/secrets-* + lib/env-cli + lib/scoped-spawn; Claude owns snapshot/lib/snapshot-* + planning docs + memory + cross-cutting framework hygiene. Operator assigns souls per lane per project.
63-05 done Wire daily tip + soul reference into Claude SessionStart. Today: scripts/generate-daily-tip.mjs and lib/teachings-reader.cjs exist but the Claude SessionStart hook chain (~/.claude/hooks/gad-check-update.js + gad-session-state.sh) does not emit them. Also: SOUL.md at repo root is readable but not auto-mentioned — Kael persona only lands when I recall CLAUDE.md pointer. Wire: (a) gad-session-state.sh emits the day&apos;s teaching tip (falls through silently if no tip), (b) new hook or same emits &apos;Active soul: &lt;name&gt; (SOUL.md)&apos; when SOUL.md present, (c) teaching generation runs per decision gad-257 at startup (dedup across multi-session-per-day), (d) llm-from-scratch subagent dispatch per gad-256/258 when appropriate. Did NOT fire for Claude this session despite decisions gad-255..258 being in place.
63-06 done Gap closed: gad phases add atomically syncs TASK-REGISTRY.xml container, gad tasks update --goal available. DONE 2026-04-18 S20 by claude-opus-4-7. Regex boundary bug in addPhaseToRegistryXml (word-boundary after closing quote) prevented idempotent no-op detection — fixed in lib/task-registry-writer.cjs. All 23 writer tests + phases-add-registry-sync tests pass. Installed gad v1.33.0 still pre-fix — 44-38 rebuild needed for distribution.
63-06a done Harden gad runtime launch new-shell path using generated temp PowerShell script files and strict argument passing; remove raw -Command one-liners.
63-06b done Fix phases add to sync TASK-REGISTRY phase containers via lib/task-registry-writer.cjs and add gad tasks update &lt;id&gt; --goal CLI path.
63-health-cli done Machine health CLI — gad health disk, gad health prune, gad health caches. Surface disk pressure + remediate from inside the loop so agents can detect OOM/ENOSPC before builds fail. (a) gad health disk reports df + du of known-hot dirs; (b) gad health prune --worktrees safely removes stale registered worktrees with no uncommitted work; (c) gad health caches --dry-run lists reproducible artifacts (build output, npm cache). Motivated by 2026-04-18 incident: cursor site build blocked by ENOSPC (2 GB free of 456 GB); 17 stale agent worktrees in .claude/worktrees/ which gad worktree list does not detect — close that gap. Acceptance: gad health disk runs under 2s on Windows without hanging du, reports top 5 hogs. gad health prune --worktrees detects orphans. gad worktree list sees claude-code agent-* worktrees.
63-graph-task-stale done Graph cache stale on TASK-REGISTRY.xml mutations. Repro 2026-04-18: (a) edit task status from planned to done in TASK-REGISTRY.xml, run gad snapshot --projectid get-anything-done, gad query &lt;id&gt; still reports (planned); (b) add a brand-new &lt;task id=...&gt; entry by hand-editing the XML, gad snapshot, gad query &lt;id&gt; returns 0 matches. Phase + decision indexes refresh fine. Root cause likely: graph extractor caches task-status + task-id index without invalidating on TASK-REGISTRY.xml mtime change, OR rebuilds only the structure not the per-task fields. Examples in the wild: 45-14 (status edit not reflected), 51-08 + 51-09 (new tasks not indexed), pre-existing entry around dev-server command bridge hardening shows up in tasks list with empty GAD-T- id. Workaround: always use &apos;gad tasks add&apos; for new tasks and &apos;gad tasks release --done --agentid --skill&apos; for status changes — both write paths apparently DO invalidate. Fix: graph extractor should mtime-watch TASK-REGISTRY.xml or always re-parse it on snapshot. Affects every agent that follows the CLAUDE.md &apos;Update TASK-REGISTRY.xml status=done&apos; workflow literally.

FIX 2026-04-18: lib/graph-extractor.cjs gains isGraphStale(planDir, jsonPath) which compares graph.json mtime against TASK-REGISTRY/ROADMAP/DECISIONS source XML mtimes, and loadOrBuildGraph(root, baseDir, opts) which is now the single read entry point — checks staleness, rebuilds + writes when stale (or when graph.json missing/corrupt), returns parsed graph. opts.readOnly skips the file-write race so concurrent snapshot agents do not collide. All 5 read sites in bin/gad.cjs (snapshot tasks block, sprint snapshot block, snapshot graph-stats block, gad tasks --graph listing, gad query run) routed through the helper. Smoke: graph.json mtime forced 10s old vs TASK-REGISTRY now → isGraphStale=true → reload rebuilt=true (1019→1023 nodes confirming a hand-edit was picked up) → isGraphStale=false.

STRENGTHENING 2026-04-19: .planning/graph.{json,html} are now gitignored — they were producing noisy diffs on every task-status change and guaranteed parallel-agent merge conflicts that were never meaningful. Cache-only model: rebuild on demand. (a) `.gitignore` added the two paths with a rationale comment. (b) `git rm --cached .planning/graph.json .planning/graph.html` untracked the historical copies. (c) bin/gad.cjs gains `ensureGraphFresh(baseDir, root)` helper near the existing `maybeRebuildGraph` — delegates to graphExtractor.loadOrBuildGraph for the staleness check (uses STALENESS_SOURCES = TASK-REGISTRY/ROADMAP/DECISIONS) and additionally writes graph.html when the json was rebuilt or the html is missing. Wired into snapshotV2Cmd (the actual snapshot dispatched at runtime — original patch hit the legacy snapshotCmd at line 9277 by mistake; corrected) and startupCmd (right after projectid resolution). Both call sites guard on !sideEffectsSuppressed() so --no-side-effects snapshots stay read-only. Smoke verified end-to-end: rm graph.* → gad snapshot prints "[snapshot] graph cache rebuilt (missing)" + writes both files; touch TASK-REGISTRY.xml → next snapshot prints "(stale)"; subsequent snapshot is silent (fresh). (d) site/scripts/pack-site-release.cjs gains ensureFrameworkGraph() that runs `node bin/gad.cjs graph build --projectid get-anything-done` before staging if .planning/graph.json is missing, so site/data/planning-graph.json (baked into the release zip by build-site-data.mjs) doesn't ship as an empty stub on a fresh checkout. Note: site/data/planning-graph.json + planning-graph-index.json remain tracked deliberately (separate site-data convention alongside cli-captures.json, db.json, etc. — those exist so static site builds are deterministic without first running the planning-graph extractor). Closes the operator-surfaced concern: "graph keeps getting modified, what is it?" — answer is now "it's a cache, you'll never see it in diffs again."
63-10 planned Drop pre-claim semantics from `gad tasks claim` — rename to attribution-only stamp written at task completion. Handoff rename is already the atomic work lock per decision 2026-04-20 D2.
63-11 planned Migrate TASK-REGISTRY.xml from single-file XML to per-task files under .planning/tasks/&lt;id&gt;.{json|md} via phased dual-write. Closes multi-agent race class. Decision 2026-04-20 D3.
63-12 planned Cut planning-app lib/planning-data.ts and site/scripts/build-site-data.mjs over to graph.json for task reads; eliminate direct TASK-REGISTRY.xml parsing in consumers. Enables 63-11 finalization. Decision 2026-04-20 D3.
63-13 planned Split STATE.xml &lt;next-action&gt; out to per-session field under .planning/sessions/&lt;id&gt;.json::next_action. Keep &lt;state-log&gt; in STATE.xml (append-only, safe). Decision 2026-04-20 D4.
63-14 planned Deprecate gad startup in favor of gad snapshot --create-session. First call of the day auto-creates a session record if none exists. Keep startup as a documented alias for one release, then remove. Decision 2026-04-20 D6.
63-15 planned Audit ROADMAP.xml canonical status. Either formalize it as source-of-truth for phases, or migrate phase definitions to .planning/phases/&lt;id&gt;/KICKOFF.md and have graph-extractor source from there. Operator had noted uncertainty about this file&apos;s role. Decision 2026-04-20 D5 follow-up.
63-16 planned Audit REQUIREMENTS.xml canonical status — is it still the official planning artifact, or has that role moved to specs/ and context-frameworks/? Decision 2026-04-20 D5 follow-up.
63-17 planned Close 3 CLI gaps in vendor/get-anything-done that surfaced 2026-04-21 from monorepo lane: (1) gad tasks show &lt;id&gt; [--projectid &lt;id&gt;] — print full task body + comments from .planning/tasks/&lt;id&gt;.json (per-task JSON canonical post-63-11). Pattern: bin/commands/tasks/list.cjs factory, add bin/commands/tasks/show.cjs, wire into tasks dispatcher. (2) gad errors --projectid &lt;id&gt; — currently returns &apos;No error attempts found&apos; even when .planning/ERRORS-AND-ATTEMPTS.xml has entries (22 entries on global today). Likely reads wrong path or wrong projectid resolver. (3) gad blockers --projectid &lt;id&gt; — returns &apos;No blockers found&apos; even when ERRORS-AND-ATTEMPTS.xml entries carry status=&apos;open&apos; or blocks=&apos;&lt;task-id&gt;&apos; attributes (2 OPEN today: gad-team-windows-detached-spawn-2026-04-21 blocks 05-07, highlight-on-child-missing-identified-wrapping-2026-04-21 blocks 05-07-phase-3). Verification: from monorepo root, gad tasks show 05-07 --projectid global prints the full goal block; gad errors --projectid global returns the 22 entries; gad blockers --projectid global returns the 2 OPEN. Lane: Codex (CLI surface). Filed via handoff per Claude-discusses/Codex-implements split.
63-18 planned Fix dispatch-infrastructure argv malformation on Windows when gad runs as bundled gad.exe sea binary. Root cause: process.execPath returns gad.exe under the sea-bundle install (%LOCALAPPDATA%/Programs/gad/bin/gad.exe) instead of node. Four spawn sites passed gad.exe + node-script-path to spawn(), causing the spawned process to re-enter gad&apos;s citty parser instead of executing as node (&apos;Unknown command C:\…\&lt;script&gt;.mjs&apos;). Sites: (1) lib/runtime-launch.cjs lines 22/28/34 — codex-trial.mjs, gemini-trial.mjs, opencode-trial.mjs (interactive runtime launch). (2) lib/team/spawn.cjs line 19 — bin/gad.cjs (worker). (3) bin/commands/team/dispatcher.cjs line 49 — bin/gad.cjs (dispatcher daemon). (4) lib/runtime-substrate-scripts.cjs line 44 — runtime substrate read-side scripts. Fix: new lib/node-exec.cjs::pickNodeExecutable() returns &apos;node&apos; when path.basename(process.execPath) is &apos;gad.exe&apos; or &apos;gad&apos;, else returns process.execPath. All four sites use the helper instead of process.execPath directly. Verified: gad.exe runtime launch --dry-run now emits &apos;node codex-trial.mjs&apos; instead of &apos;gad.exe codex-trial.mjs&apos;; gad team start --n 1 --runtime codex-cli now reaches IDLE with live PID; gad team dispatch routes handoffs into worker mailbox; w1 transitions to WORKING. Resolves entry gad-team-windows-detached-spawn-2026-04-21 (was OPEN, blocked task 05-08 in monorepo global registry). Constraint: assumes &apos;node&apos; is on PATH where gad.exe was installed (almost always true since gad.exe install requires node-shaped CI). Lane: cross-lane (claude-code touched vendor runtime/spawn surface — operator-blessed because dispatch was self-blocking; couldn&apos;t go through Codex without a working dispatch path).
63-19 planned Fix worker-loop body-loading. lib/handoffs.cjs::claimHandoff returns destPath (string), but lib/team/worker-loop.cjs:103 read .body off it expecting an object. Result: every handoff dispatched via gad team got an empty body, prompt fell back to &apos;(body not loaded)&apos; → codex either guessed or hung. Fix: call readHandoff() after claimHandoff() to load body separately. Validated: foreground worker test with the fix prints actual handoff body in prompt file. Discovered while dispatching VCS extraction Phase 1 (h-2026-04-21-VCS-extraction-phase-1) where the empty prompt + codex hooks hang killed the run.
63-20 planned Drop AI-written heartbeat anti-pattern. lib/team/worker-loop.cjs writes last_heartbeat at top of each iteration via updateStatus() — wasted I/O AND misleading (tracks worker-loop cadence not codex liveness; loop blocks on codex&apos;s runSubprocess await for the whole task duration). Per memory project_log_inference_liveness: liveness inferred from log.jsonl mtime + last subproc-stderr ts. Fix: (a) drop last_heartbeat write from worker-loop, (b) gad team status HB(s) computes from log.jsonl mtime instead, (c) GAD TUI workers panel tails log.jsonl + status from there. Operator quote 2026-04-22: &apos;I didn&apos;t want the AI personally writing a heartbeat. that is inefficient. but honestly if you just launch a fleet and can see them interactively in the GAD TUI we should be monitoring via that.&apos;
63-21 planned Two related team-spawn fixes: (1) gad team start --projectid global from monorepo cwd silently kills the detached worker — phantom pid returned, NOT_STARTED state, status.json never written. Workaround proven: --no-spawn + manual node bin/gad.cjs team work background launch. Instrument lib/team/spawn.cjs::spawnWorker with eager error logging. Try windowsVerbatimArguments + argv0. Fall back to non-detached spawn with cmd start /B wrapper on Windows. (2) When runtime=codex-cli auto-inject headless flags into runtime_cmd: &apos;-c features.codex_hooks=false -c notify=[]&apos;. Without these OMX notify-hook hangs codex in detached worker (zero output for 20+ min). See ERRORS codex-hooks-hang-detached-worker-2026-04-22.
63-22 planned GAD TUI workers liveness panel + subprocess watchdog. (1) packages/gad-tui add /workers route that tails log.jsonl per worker, computes liveness from mtime, displays badges [WRITING] [SILENT &gt;Ns] [WORKING] [IDLE] [DEAD]. Operator never needs to arm bash Monitor by hand. (2) lib/team/subprocess.cjs::runSubprocess add stderr-mtime watchdog — if no subproc activity &gt;300s and no child.exit, force-kill + logWrite work-failed reason=watchdog-timeout. Resolves silence-as-failure class (entries codex-worker-no-completion-marker, monitor-silence-not-failure, codex-hooks-hang-detached-worker).
63-23 planned Decision GLOBAL-D-05-02 reference: TUI is the operator&apos;s interactive coordinator (chat + dispatch + viewer + commands), not a viewer. It must NEVER refuse to start. Multi-instance allowed; if a router is already running for this workspace, attach + share state instead of competing.

REPRO (2026-04-22): operator ran &apos;gad tui&apos;. Got: &apos;[router] lockfile exists at C:/Users/benja/Documents/custom_portfolio/.gad/router-logs/.lock — pid=26520 is still running.&apos; Verified PID 26520 NOT alive (tasklist empty). Lockfile timestamp 5h+ stale. Operator forced to manually &apos;rm .gad/router-logs/.lock&apos; to launch. ERRORS entry: gad-tui-stale-router-lockfile-blocks-launch-2026-04-22.

ROOT CAUSE: TUI is currently bundled with router (start TUI → start router → singleton lock at .gad/router-logs/.lock). Two problems: (a) lockfile orphans when prior TUI crashes without cleanup, (b) operator can&apos;t peek at active workers while another TUI session is open (prevents the &apos;I just want to look while claude works&apos; flow).

FIX SCOPE: split TUI from router. Three pieces:
(1) Router becomes a standalone singleton process per workspace (or per-project — see 63-24). Lockfile still valid for router, but with eager liveness probe before refusing: parse pid from lockfile, probe via process.kill(pid, 0) on POSIX or &apos;tasklist /FI &quot;PID eq &lt;pid&gt;&quot;&apos; on Windows. If pid dead OR lockfile age &gt; N seconds AND pid dead, log &apos;[router] stale lockfile (pid=X dead, age=Ns) — clearing&apos; and unlink + proceed.
(2) TUI becomes standalone — no lockfile of its own. On launch: probe for running router (read .gad/router-logs/.lock, check liveness). If running: ATTACH (tail router-logs, talk to router via IPC — named pipe / unix socket / shared file). If not running: spawn one as detached background process AND open TUI as separate viewer.
(3) Files to inspect: packages/gad-tui/src/router.ts (lockfile logic at lines 18, 19, 54, 55, 256, 257, 356), packages/gad-tui/src/server/serve.ts, packages/gad-tui/src/server/TuiServer.ts.

EDGE CASES:
- Lockfile exists, pid alive, lockfile age &lt; 60s: another TUI just opened; attach.
- Lockfile exists, pid alive, lockfile age &gt; some threshold: still attach (long-running TUI is normal — operator left it open).
- Lockfile exists, pid dead, lockfile age any: clear + start fresh router + open TUI.
- Lockfile missing: start fresh router + open TUI.
- Multiple TUIs concurrently: all attach to the same router; each sees same state.
- IPC failure (router alive but doesn&apos;t respond): timeout after Ns, log warning, fall back to read-only mode (TUI still opens, just can&apos;t dispatch).

ACCEPTANCE: &apos;gad tui&apos; command never exits with a lockfile-related error. Two concurrent &apos;gad tui&apos; processes both render full state. Killing one doesn&apos;t affect the other. After all TUIs close, router stays running until explicit &apos;gad router stop&apos; or workspace close.

PID-FILE SWEEP (separate audit): all vendor pid-file consumers (gad team dispatcher .planning/team/dispatcher.pid, possibly others) need the same eager-probe pattern. Grep for &apos;pidfile\|\.pid\|lockfile\|\.lock&apos; in vendor/get-anything-done/lib + bin to find them.

REFERENCES:
- ERRORS: gad-tui-stale-router-lockfile-blocks-launch-2026-04-22 (open, blocks this task)
- DECISION: GLOBAL-D-05-02 — every project is a GAD project, TUI is the unified coordinator
- MEMORY: project_phase_64_tui_orchestrator (TUI is operator&apos;s primary interactive surface)
63-24 planned Decision GLOBAL-D-05-02 reference: every project is a GAD project. All per-project infrastructure scopes to the project&apos;s planning root — no exceptions, no cwd specialness.

PROBLEM: today there are infrastructure files at the wrong scope:
(1) .gad/router-logs/ at MONOREPO ROOT cwd, not per-project. Means project 1&apos;s router and project 2&apos;s router can&apos;t run concurrently — they fight over the same lockfile + log dir.
(2) Possibly other pid-files / lockfiles / logs scoped to cwd instead of resolved projectid.
(3) Confirmed working / per-project already: &lt;project&gt;/.planning/team/workers/, &lt;project&gt;/.planning/team/dispatcher.* — those scope correctly. The router is the asymmetry.

ROOT CAUSE: packages/gad-tui/src/router.ts hardcodes &apos;process.cwd()&apos; for logDir + lockPath at lines 54-55, 256-257. Router doesn&apos;t know about projectid resolution. Whatever cwd the router was launched in becomes its scope.

REPRO of the asymmetry it causes: ERRORS team-start-detached-spawn-projectid-global-fails-2026-04-22 — gad team start --projectid global from monorepo cwd silently kills detached worker, but --projectid get-anything-done from same cwd works. Symptom of cwd asymmetry.

FIX SCOPE:
(1) Audit every vendor file that reads/writes a pid-file, lockfile, or log dir. Search vendors for: &apos;pidfile&apos;, &apos;\.pid&apos;, &apos;lockfile&apos;, &apos;\.lock&apos;, &apos;router-logs&apos;, &apos;.gad/&apos;. Probable hits: packages/gad-tui/src/router.ts, vendor/get-anything-done/lib/team/dispatcher.cjs (already scoped via baseDir, verify), vendor/get-anything-done/lib/team/spawn.cjs.
(2) For each, accept --projectid (or default to active session project). Resolve project&apos;s planning root via the same gadConfig.load + resolveRoots helpers vendor team commands use. Move all infra under &lt;project-root&gt;/.gad/ + &lt;project-root&gt;/.planning/.
(3) Backwards compat: if old paths exist (.gad/router-logs/.lock at cwd), migrate or warn. After one release cycle, remove the cwd fallback.

EDGE CASES:
- Project with planning root = &apos;.&apos; (e.g. global) — .gad/ goes at monorepo root, same physical location as today, just resolved via projectid not cwd.
- Project with planning root = &apos;vendor/X&apos; — .gad/ goes at &apos;vendor/X/.gad/&apos;, not monorepo root.
- gad command run with NO --projectid + NO active session: error clearly (&apos;no projectid resolved — run gad startup --projectid X first&apos;), don&apos;t fall back to cwd.
- Symlinked project paths: resolve symlinks consistently (fs.realpathSync) so two ways to reach the same project don&apos;t create two router instances.
- Concurrent project routers: each holds its own .gad/router-logs/.lock at its own scope, no contention.

ACCEPTANCE:
- &apos;gad team start --projectid global&apos; from monorepo cwd works identically to &apos;gad team start --projectid get-anything-done&apos; from same cwd. No silent spawn death. Same liveness, same ergonomics.
- Two routers running concurrently for two different projects, each with its own lockfile + logs, neither blocking the other.
- &apos;find . -name .lock -o -name .pid -o -name &apos;*.pid&apos;&apos; shows no infra at the wrong scope.

REFERENCES:
- ERRORS: team-start-detached-spawn-projectid-global-fails-2026-04-22 (open, blocked by this)
- DECISION: GLOBAL-D-05-02
- DEPENDS: 63-23 (TUI never refuses launch — must land first so per-project routers don&apos;t get blocked by stale per-project lockfiles)
63-25 planned Decision GLOBAL-D-05-02 reference: TUI is the unified interactive coordinator across all projects in a workspace. Operator can have project A&apos;s fleet AND project B&apos;s fleet running concurrently and view/control both from one TUI.

PROBLEM TODAY: TUI is single-project at best. No way to see &apos;global has 1 worker IDLE, get-anything-done has 3 workers WORKING, llm-from-scratch has fleet stopped&apos; all at once. Operator has to switch contexts mentally + run gad team status --projectid X for each project to know what&apos;s happening.

USE CASE (operator&apos;s words): &apos;I want people to be able to use differing runtimes in the desktop app for differing projects. like if project 1 wanted a fleet and project 2 wanted a fleet, we would be able to work on both of them with differing fleets of workers at the same time.&apos; AND &apos;I just wanted it for monitoring when you are doing things behind the scenes yourself. I just want to have a look.&apos;

FIX SCOPE: TUI gains a Workspace View as its top-level orientation:
(1) On launch, TUI iterates gad-config.toml [[roots]] entries (workspace-scoped — see DECISION GLOBAL-D-05-02 answer A). For each project, render a row/card with: name + projectid + planning-root + router state (idle/running/no-fleet) + worker count by role + total mailbox depth + last activity timestamp.
(2) Per-project controls inline: &apos;Start fleet&apos; (opens profile picker — default 1 worker, opt-in --profile loads N workers with roles), &apos;Stop fleet&apos;, &apos;Dispatch handoff&apos;, &apos;Open project detail view&apos; (drills into that project&apos;s full state).
(3) Live updates via tailing log.jsonl files + status.json file watches (chokidar or fs.watch). No polling.
(4) Worker roles visible: when a fleet uses --profile (loads N workers with lanes like &apos;ui&apos;, &apos;backend&apos;, &apos;reviewer&apos;), display each worker&apos;s role tag in the unified view.
(5) Cross-project dispatch (advanced): operator can drag a handoff from project A&apos;s queue to project B&apos;s queue if it&apos;s mis-routed. Optional Phase 2.

ARCHITECTURE NOTES:
- Don&apos;t change the data model — projects, fleets, workers are already per-project. This is a presentation + control layer over existing primitives.
- Build on the new TUI/router split from 63-23 + per-project scoping from 63-24. Both must land first (depends).
- The fleet &apos;shape&apos; (1 vs N workers) is the existing worker_spec[]. Default --n=1, opt-in --profile loads the saved spec.

EDGE CASES:
- Project listed in gad-config.toml but planning root missing: render as &apos;unconfigured&apos;, let operator click &apos;gad startup&apos; inline.
- Project&apos;s router not running: render &apos;no fleet&apos;. &apos;Start fleet&apos; button is the entry point.
- Project&apos;s router running but no workers: render &apos;router idle, 0 workers&apos;. &apos;Start fleet&apos; adds workers.
- Worker NOT_STARTED state &gt;60s old: show as warning (&apos;worker w1 stuck NOT_STARTED — likely spawn issue, see 63-21&apos;). Operator can &apos;Restart worker&apos;.
- Worker WORKING but log.jsonl untouched &gt;300s: show as warning (&apos;worker w1 silent — codex may be hung, see 63-22&apos;). Operator can &apos;Force-kill worker&apos;.
- Multiple TUIs open concurrently (per 63-23): all see same workspace view, all updates propagate.

ACCEPTANCE:
- &apos;gad tui&apos; launched in monorepo workspace shows global, get-anything-done, llm-from-scratch, magicborn, etc — every project from gad-config.toml [[roots]].
- Each project&apos;s row/card shows live state without manual refresh.
- Starting fleets in two different projects concurrently produces two non-conflicting fleets, both visible.
- Operator can inspect any worker&apos;s logs without leaving the TUI.

REFERENCES:
- DECISION: GLOBAL-D-05-02
- DEPENDS: 63-23 (TUI never refuses), 63-24 (per-project router scoping)
- MEMORY: project_phase_64_tui_orchestrator (TUI is operator&apos;s primary interactive surface)
- MEMORY: project_log_inference_liveness (no polling, watch logs for state)
63-26 planned PROBLEM: gad-tui crashes during ctrl+n cycling with &apos;undefined is not an object (evaluating task.title.length)&apos; at gad-tui-windows.exe:34219.

REPRO (operator 2026-04-22): launched gad tui, used ctrl+n to cycle panes, immediately crashed.

OFFENDING CODE (from stack trace):
  const title60 = task.title.length &gt; 60 ? task.title.slice(0, 57) + &apos;\u2026&apos; : task.title;

ROOT CAUSE: no null-check on task.title. A task entry has title=undefined or task itself is undefined. Probable culprits:
(1) Per-task JSON file written without title field (gad tasks add doesn&apos;t require title — only goal). Task display layer assumes title exists.
(2) Task index (graph.json or similar) returning malformed entry — synthesis from XML/JSON merge has a hole.
(3) ctrl+n iterates a list that includes a placeholder/sentinel without title.

FIX SCOPE:
(1) Defensive at the render site:
  const title = task.title ?? task.goal?.split(&apos;\n&apos;)[0]?.slice(0, 80) ?? task.id ?? &apos;(untitled)&apos;;
  const title60 = title.length &gt; 60 ? title.slice(0, 57) + &apos;\u2026&apos; : title;
  Use goal first line as title fallback before id, since most tasks have a goal but not all have a title.
(2) Find the source — grep packages/gad-tui/src for &apos;task.title&apos; references. Identify which read path produces the undefined-title task. Likely lib/team/task-loader or similar reading per-task JSONs.
(3) Audit task writers (gad tasks add, gad tasks update, gad tasks promote) to confirm they all set title or have a default. Migration script for existing tasks: scan .planning/tasks/*.json, for any without title, derive from goal first line + write back.

EDGE CASES:
- Task object itself undefined (not just title): defensive prefix &apos;(task &amp;&amp; (task.title ?? …))&apos;.
- Task.title is empty string &apos;&apos;: treat as missing, fall back to goal/id.
- Task.title is a number / non-string: stringify first.
- Tree mode (treeMode flag from stack trace) showing root nodes that aren&apos;t real tasks (synthetic group headers): these should have a title set by the tree-builder.

ACCEPTANCE:
- ctrl+n cycling never crashes.
- All tasks render with SOME title (one of: title, goal first line, id, or &apos;(untitled)&apos;).
- Task indexer warns when it loads a task without title (so we can fix the writer not the reader).

REFERENCES:
- ERRORS: gad-tui-ctrl-n-task-title-crash-2026-04-22 (will add this turn)
- File: gad-tui bundle line 34219 (source: probably packages/gad-tui/src/components/tasks/ or similar)
63-27 planned PROBLEM: gad-tui layout reflows dramatically + content jumbles when cycling panes via ctrl+n. Operator quote 2026-04-22: &apos;the UI also shifts dramatically when doing a ctrl+n and jumbles up. the actual transcript/header and etc space just isnt as wide as the status bar, the chat input and etc just need to be wider I think.&apos;

REPRO: launch gad tui in any sufficiently-wide terminal. ctrl+n to cycle panes. Notice: status bar (full width) vs transcript/header/chat-input (narrower). Layout shifts on each pane switch instead of staying stable.

ROOT CAUSE HYPOTHESES (verify in code):
(1) Inner column widths use a stale/initial-mount terminal width vs a recomputed one. Width recomputed on resize event but not on pane switch.
(2) Flex/grid template biased toward one child — sidebar or status pane stealing horizontal space from main column.
(3) Different panes use different layout templates — switching to a &apos;narrow content&apos; pane shrinks the outer frame&apos;s child constraints.
(4) Border / padding accounting wrong: status bar excludes padding, content includes (or vice versa) → off-by-N visible.

FIX SCOPE:
(1) Inspect packages/gad-tui/src/layout (or wherever the root layout lives — find via grep &apos;transcript\|chat-input\|status-bar&apos; in src).
(2) Make the layout invariant: outer frame fixed to terminal width, status bar and main column share the same width, chat input fills main column width.
(3) Recompute layout on pane switch (not just terminal resize). Or use a layout primitive (Ink Box with width=&apos;100%&apos;) that handles it.
(4) Check that ctrl+n pane switch isn&apos;t unmounting + remounting the layout root (would re-init children with wrong sizes).

EDGE CASES:
- Terminal width changes during session: layout must reflow without jumble (existing behavior for resize).
- Very narrow terminal (&lt;80 cols): collapse to single-column layout vs jumbling.
- Pane with optional sidebar (e.g. fleet view): sidebar should overlay or split, not shift outer frame.
- Tasks panel + chat panel different heights: don&apos;t let one steal the other&apos;s vertical space mid-cycle.

ACCEPTANCE:
- Status bar, header, transcript, chat input ALL at the same width as the outer terminal frame (modulo intentional padding).
- ctrl+n cycling produces a stable layout — only the active-pane indicator changes, not the column widths.
- Operator stops noticing layout jumble.

REFERENCES:
- ERRORS: gad-tui-ctrl-n-layout-shift-2026-04-22 (will add this turn)
- Likely files: packages/gad-tui/src/components/, packages/gad-tui/src/layout/ (verify with grep)
63-28 planned Layer superpowers-style per-runtime manifest emission on top of GAD&apos;s existing copy-install (hybrid architecture per GSD+superpowers review 2026-04-22). Today gad install --claude/--cursor/--codex/--gemini/--opencode copy-transforms skills into each runtime&apos;s expected dir via bin/install.js (5,687-line fork of GSD&apos;s install.js). Consumer updates stomp customizations; catalog 93 skills × N runtimes = massive copy surface. Hybrid approach: KEEP copy-install as fallback for plugin-less runtimes (aider, continue, roo, others that don&apos;t speak plugin manifests); ADD per-runtime manifests for PLUGIN-CAPABLE runtimes (Claude Code, Cursor, Gemini CLI, Codex, Opencode — these runtimes accept plugin manifest files as native load mechanism). Scope: (1) author .claude-plugin/plugin.json + marketplace.json (per superpowers pattern), (2) .cursor-plugin/plugin.json, (3) gemini-extension.json + GEMINI.md (Gemini CLI uses @./ imports for context files), (4) AGENTS.md as Codex pointer (minimal: just point at CLAUDE.md — superpowers does a 9-byte symlink), (5) .opencode/plugins/gad.js mutating opencode config singleton, (6) hooks/session-start polyglot script + hooks.json / hooks-cursor.json env-var-switched JSON emitters, (7) .version-bump.json + audit script keeping manifests in lockstep (superpowers&apos; bump-version.sh pattern), (8) refactor bin/install.js to emit manifests for plugin-capable runtimes instead of copy-transform (copy stays for fallback runtimes). DEPENDENCIES: 63-43 (GitHub Releases pipeline must work first — manifests need a release source to point consumers at; GAD is PRIVATE code per gad-188 so never public npm, but Claude marketplace accepts git-hosted or tarball sources) + 63-42 (GSD marketplace abandonment investigation — know why they dropped it before we walk the same path) + 63-44 (install.js refactor strongly recommended — don&apos;t add manifest logic to the 5,687-line monolith). Distribution can also use GitHub Packages scoped @magicbornstudios/get-anything-done if a plugin-capable runtime requires npm-installable — private registry only, never public. Gaps to fill that superpowers doesn&apos;t: automated Cursor/Codex/Gemini test harnesses (superpowers ships with manual-only), Windows-friendly hooks (superpowers uses bash with silent Windows failover via run-hook.cmd polyglot). References: .planning/notes/2026-04-22-superpowers-review.md + .planning/notes/2026-04-22-gsd-distribution-review.md. DO NOT propose public npm publishing — decision gad-188 forbids it, operator has corrected this 20+ times.
63-29 planned Skills audit — classify every skill in the catalog as (real skill / CLI wrapper / stale / duplicate) and collapse. Catalog size today: 92 canonical skills in vendor/get-anything-done/skills/ plus 283 paths across .claude/skills, .cursor/skills, .codex/skills, .agents/skills runtime mirrors. Operator 2026-04-22: &apos;I am guessing we havent done our skills audit, removing duplicates and reducing our runtime folders to just bare minimum.&apos; For each canonical SKILL.md classify: (a) REAL SKILL — structured multi-step workflow with allowed-tools, artifact contracts, subagent spawning, hook-relevant boundaries. Keep. (b) CLI WRAPPER — skill that only runs a gad subcommand and returns; no decisions, no substeps. Collapse into CLI or delete. (c) STALE — references deleted phases, replaced workflows, deprecated commands. Delete. (d) DUPLICATE — overlaps with another skill 80%+. Merge. Output: .planning/notes/2026-04-22-skills-audit.md with per-skill verdict + action + migration plan. Expect ~30-40% reduction. Feeds 63-28 (plugin content) and 63-30 (MCP tool server).
63-30 planned MCP server exposing gad CLI as runtime-agnostic tools — separate track from skills. Operator 2026-04-22 asked about mcp_skills approach. Clarification: MCP is for TOOL EXPOSURE across runtimes (codex, cursor, gemini, opencode can all consume it), NOT for skill enforcement (that&apos;s Claude Code Skill()). Scope: MCP server gad-mcp exposing tools (gad_snapshot, gad_state_log, gad_tasks_add, gad_tasks_stamp, gad_decisions_add, gad_handoffs_create, gad_state_set_next_action, gad_phase_show, gad_skill_list, gad_skill_show, etc.). One server, any MCP-speaking runtime discovers + calls. Existing bin/gad-mcp.cjs is the starting point — audit its current tool list, extend to cover the canonical subcommand surface, document in references/mcp-tools.md. Does NOT replace skills — MCP tools are stateless capability calls, skills are structured workflows.
63-31 planned PostToolUse hook on Skill tool → auto-attribution via gad tasks stamp-skill. Operator 2026-04-22: &apos;if you have a tool called Skill() how come you have not been able to give good attribution via hooks for pretool or posttooluse to capture those skills and instead have made me rely on telling agents to manually attribute skills.&apos; Valid gap. Action: (a) add bin/commands/tasks.cjs subcommand &apos;gad tasks stamp-skill --from-hook&apos; that reads hook payload from stdin (Claude Code passes tool name + args) + active session id + current task id from .planning/sessions/&lt;id&gt;.json, then appends skill to the task&apos;s stamped-skills list, (b) document settings.json hook snippet registering PostToolUse on &apos;Skill&apos; → shell out to gad tasks stamp-skill --from-hook, (c) add a parallel for MCP tool calls pattern-matched on &apos;mcp__gad__*&apos; names, (d) test by invoking a skill and verifying registry reflects the attribution without agent intervention. Closes the manual-stamp loop.
63-32 planned Snapshot output: replace &apos;top 5 skills by relevance&apos; block with per-phase recommended skills. Today gad snapshot shows 5 equipped skills based on global relevance scoring. Operator 2026-04-22: &apos;maybe showing the list of available skills on snapshot would be good.&apos; Per-phase is more useful than global top-N because different phases pull different toolchains. Scope: (a) extend phase metadata to carry an optional &apos;recommended-skills&apos; list (editable via gad phase CLI), (b) snapshot reads the active phase&apos;s recommended-skills and shows those plus a 2-line &apos;available for this phase&apos; footer, (c) fallback to the current top-5 if no per-phase list is declared, (d) auto-detect logic: skills that appeared in the phase&apos;s task stamp-history count as recommended candidates. Cosmetic but load-bearing — snapshot is the orientation call, not a skill discovery call.
63-33 planned Re-verify skills audit COLLAPSE verdicts against workflow file content (not just SKILL.md body). The 2026-04-22 audit flagged 28 skills COLLAPSE based on thin SKILL.md bodies, but skills with AskUserQuestion in allowed-tools, workflow files &gt;100 lines, or artifact contracts in &lt;objective&gt; tags may be real structured skills whose body is thin per the canonical skill shape (decision gad-190). Known false positives already identified: gad-plant-seed (structured interactive capture with deferred consumption), gad-auto-conventions (codebase analysis → synthesis → write), gad-write-feature-doc (needs check). Scope: for each of the 28 COLLAPSE skills, (a) read its workflow file if referenced, (b) check allowed-tools for AskUserQuestion/Task/Write/Edit, (c) check for artifact contracts declared in objective or process tags, (d) reclassify as KEEP or confirm COLLAPSE. Output: .planning/notes/2026-04-22-skills-audit-reverify.md updating each verdict with evidence. Expected ~5-10 skills move from COLLAPSE back to KEEP. Also drop any proposed CLI additions that no longer make sense after reclassification.
63-34 planned Deep review of tmp/superpowers/ (obra/superpowers) for runtime-agnostic plugin pattern. Cloned 2026-04-22 at tmp/superpowers/. Key artifacts to examine: (a) .claude-plugin/plugin.json + marketplace.json — Claude Code manifest shape, (b) .cursor-plugin/plugin.json — Cursor manifest shape, (c) gemini-extension.json + GEMINI.md — Gemini CLI extension pattern, (d) .opencode/plugins/superpowers.js — opencode plugin shape, (e) AGENTS.md — Codex convention, (f) hooks/hooks.json + hooks/hooks-cursor.json — per-runtime hook definition, (g) scripts/ — any install/sync tooling, (h) skills/ layout — how per-skill dirs are organized, what frontmatter pattern is used, which tools declare allowed-tools, (i) package.json — version strategy, (j) tests/ — what they test for runtime compatibility. Deliverable: .planning/notes/2026-04-22-superpowers-review.md with (1) per-runtime manifest template we can adapt, (2) skill-dir layout recommendations, (3) hook-definition pattern, (4) install flow per runtime, (5) what GAD would need to change to adopt this pattern, (6) gaps (things superpowers doesn&apos;t solve that GAD does — e.g., planning state, task registry, session handoffs). Feeds 63-28 (GAD as Claude Code plugin — now re-scoped to runtime-agnostic plugin).
63-35 planned Delete gad-cleanup skill; fold its milestone-archival logic into gad-complete-milestone. gad-cleanup references deprecated .planning/MILESTONES.md (not in canonical schema which uses .planning/milestones/v{X.Y}-*.md). gad-complete-milestone already archives as part of its 8-step pipeline. Action: (1) audit gad-cleanup workflow for any archival step not already in gad-complete-milestone, (2) port missing logic into gad-complete-milestone, (3) update its SKILL.md/workflow to reflect absorbed scope, (4) delete vendor/get-anything-done/skills/gad-cleanup/ + vendor/get-anything-done/workflows/cleanup.md, (5) grep repo for any references and remove. Operator confirmed 2026-04-22.
63-36 planned Delete gad-migrate-schema skill — vestigial. Workflow converts XML→MD but current canonical direction is the opposite (GAD writes XML: TASK-REGISTRY.xml, STATE.xml, DECISIONS.xml, ERRORS-AND-ATTEMPTS.xml, ROADMAP.xml). Either a pre-XML-era one-time migration artifact or misnamed intent — no current use. Operator confirmed 2026-04-22 as vestigial. Action: delete vendor/get-anything-done/skills/gad-migrate-schema/ + workflows/migrate-schema.md, grep repo for references, remove.
63-37 planned gad-milestone umbrella skill — keep as --audit/--close/--new/--plan-gaps/--summary dispatcher. 238-line workflow has methodology the three sub-skills (gad-audit-milestone, gad-complete-milestone, gad-new-milestone) may or may not carry in full. Action: (1) audit the umbrella workflow against the 3 (now 4 after 63-35 fold, potentially 5 if gad-plan-milestone-gaps + gad-milestone-summary fold in as subverbs) to identify unique methodology, (2) either port unique methodology into the real skills and make the umbrella a thin router, OR keep the umbrella as canonical and have sub-skills be views of it, (3) document the decision in gad-milestone SKILL.md body as &apos;routes to: --new/--complete/--audit/--plan-gaps/--summary&apos;. Operator confirmed &apos;do recommended&apos; 2026-04-22.
63-38 planned Rename gad-eval-spawn to align with gad-212 vocabulary refresh. Operator 2026-04-22: &apos;no idea what orchestration you are talking about. but we can rename.&apos; Clarification: gad-eval-spawn is NOT a pure CLI wrapper — it performs 9-step orchestration (parse args, run &apos;gad eval run --execute&apos; to get EXEC.json, refuse-if-duplicate, create git worktree, write .planning/.eval-runs/&lt;id&gt;.json session marker, spawn general-purpose subagent with isolation=worktree + prompt from EXEC.json, on completion run &apos;gad eval preserve&apos;, clean worktree, report status). The subagent dispatch + worktree isolation + session marker + cleanup IS the orchestration — CLI is called mid-flow, not around it. Action: rename skill + workflow + frontmatter from gad-eval-spawn → gad-generation-spawn (matches decision gad-219 &apos;gad generation spawn&apos; CLI verb). Update body to use species/generation vocabulary throughout. Keep the orchestration intact — it&apos;s the canonical plan/build seam per decision forge-09.
63-39 planned Delete gad-pause-work + gad-resume-work skills; fold HANDOFF.json resumption logic into gad-handoffs. Operator 2026-04-22: &apos;yeah those can just be part of handoffs.&apos; gad-pause-work already self-describes as &apos;thin wrapper around gad-handoffs&apos;; gad-resume-work delegates to resume-project.md (326 lines) which has substantive HANDOFF.json parsing + incomplete-plan detection + interrupted-agent resumption + STATE.md reconstruction from artifacts + 5 next-action routes. Action: (1) audit resume-project.md for logic not already in gad-handoffs (HANDOFF.json parse, STATE reconstruction from artifacts, next-action route decision, incomplete-plan detect), (2) port missing pieces into gad-handoffs (new sub-verbs as needed: &apos;gad handoffs resume &lt;id&gt;&apos;, &apos;gad handoffs reconstruct-state&apos;, or integrate into existing claim/claim-next), (3) strip .continue-here.md references (retired pattern), (4) delete vendor/get-anything-done/skills/{gad-pause-work,gad-resume-work}/ + workflows/{pause-work.md,resume-work.md,resume-project.md}, (5) grep repo for references + update.
63-40 planned Vocabulary refresh pass on objective-eval-design + other skills per decision gad-212 (eval → species/generation). Concrete path for each affected skill: (1) read SKILL.md + referenced workflow, (2) identify every literal &apos;eval&apos; / &apos;evals/&apos; / &apos;/runs/&apos; / &apos;eval_mode&apos; / &apos;eval project&apos; reference, (3) substitute per context: &apos;eval&apos; as configuration/project-variant → &apos;species&apos;; &apos;eval&apos; as produced-artifact → &apos;generation&apos;; &apos;eval run&apos; command → &apos;gad generation spawn&apos; per gad-219; &apos;evals/&lt;project&gt;/&apos; path → &apos;species/&lt;project&gt;/&apos; (or per current canonical layout post-rename); &apos;eval_mode&apos; field → drop (dropped per decision 43-03); PRESERVE &apos;evaluation&apos; where academic methodology term (rubric/criteria); PRESERVE &apos;eval&apos; where it&apos;s part of a proper noun like &apos;cli-efficiency evals&apos; historical data, (4) update CLI references from &apos;gad eval X&apos; to &apos;gad species X&apos; / &apos;gad generation X&apos; per decision gad-219 command-vocabulary decision, (5) update filesystem path references, (6) commit per skill. Target skills (beyond the 6 gad-eval-* wrappers being deleted): objective-eval-design, self-eval, eval-skill-install. Deliverable: 3 skill bodies refreshed, commits per skill, cross-ref updated in related notes/decisions.
63-41 planned Delete gad-auto-conventions skill — redundant with gad-map-codebase. gad-auto-conventions produces a single .planning/CONVENTIONS.md via codebase pattern scan. gad-map-codebase produces 7 structured documents in .planning/codebase/ via 4 parallel subagents — CONVENTIONS.md is one of its outputs (Agent 3 quality focus). Operator 2026-04-22: &apos;sounds like map codebase to me.&apos; Correct: gad-auto-conventions is a subset of gad-map-codebase. Action: (1) confirm gad-map-codebase&apos;s CONVENTIONS.md output covers gad-auto-conventions&apos; intent (naming, structure, imports, state, components, types, tests), (2) add an optional --focus=conventions arg to gad-map-codebase that runs only the conventions mapper agent if operator wants the narrow output, (3) delete vendor/get-anything-done/skills/gad-auto-conventions/ + workflows/auto-conventions.md, (4) grep repo references + update.
63-42 planned Investigate why GSD dropped Claude marketplace plugin support between changelog entry 2026-01-06 (&apos;Claude Code marketplace plugin support&apos;) and 1.35.0 source tree (manifest no longer present). Unknown abandonment reason; superpowers successfully ships on marketplace so the mechanism works. Possible causes: (a) review rejected by Anthropic, (b) submission process friction, (c) plugin conflicts with existing install.js copy-install, (d) maintenance burden of dual-distribution, (e) marketplace policy change. Action: (1) git log + git show on the GSD repo to find the add + remove commits, (2) read commit messages + any linked issues, (3) check if the removed plugin.json + marketplace.json still exist in history, (4) document findings in .planning/notes/2026-04-22-gsd-marketplace-abandonment.md. If the cause is (a) or (e), we need to know before attempting the same path. If (b)-(d), we can proceed differently.
63-43 planned Stand up the GitHub Releases distribution pipeline per decision gad-188 #1 (&apos;NO npm publishing ever — we ship from GitHub releases of github.com/MagicbornStudios/get-anything-done&apos;). GAD is PRIVATE code not intended for public npm. Operator has corrected this 20+ times. Overlaps with task 44-38 (44-28.B5) which rewrites /gad-update from &apos;npm view/install&apos; to &apos;gh release download &lt;tag&gt; + tarball unpack + bin/install.js&apos;. Current build pipeline: scripts/build-bun-release.mjs uses bun build --compile (supersedes decision gad-138&apos;s Node SEA approach — gad-138 is stale, needs new decision). Ships gad.exe + gad-tui.exe. Scope: (1) confirm gh release pipeline exists — gad-v1.32.0-windows-x64.exe was built per gad-188 impact note &apos;3 days stale and has never been published anywhere&apos;, so the publish step itself may be missing, (2) author local operator-triggered release script (scripts/release.mjs) that version-bumps + runs build-bun-release + gh release create + uploads artifacts, (3) rewrite gad-update workflow to pull from GitHub Releases API via gh CLI (tarball OR bun-compiled binary), (4) document install path: curl/wget from release URL OR gh release download + unpack + run bin/install.js, (5) PRIVATE alternatives evaluated + documented: GitHub Packages scoped @magicbornstudios/get-anything-done, private npm registry, direct tarball hosting — pick secondary channel if plugin-capable runtime requires npm-installable, (6) validate end-to-end on a clean machine. Prerequisite for 63-28 plugin manifests (Claude marketplace accepts git-hosted or tarball, not just npm).
63-44 planned Refactor bin/install.js (5,687 lines) from monolith to strict &apos;one file = one concern&apos; modular architecture. Direct fork of GSD&apos;s install.js (6,667 lines); both projects treat this as their biggest maintenance liability. Operator 2026-04-22: &apos;i dont want a 6k install.js so that will need to be refactored to 1 file = one concern and high reusability and modularity to avoid agents stepping on each others toes.&apos; Modularity discipline: (1) each file has ONE concern and ONE public entry point, (2) files small enough that parallel agents can edit different files simultaneously without merge conflicts (target: &lt;300 lines per module, not &lt;500), (3) cross-module calls via explicit imports, no shared mutable state, no global state mutation, (4) per-runtime install LOGIC in bin/install/runtimes/&lt;runtime&gt;.cjs (one file per runtime: claude, cursor, codex, gemini, opencode, aider, continue, roo, + others), (5) CONVERTERS (tool-name translation, path rewriting, frontmatter mutation) in bin/install/converters/&lt;type&gt;.cjs (one file per converter concern — toolname, paths, frontmatter, etc.), (6) SHARED HELPERS in bin/install/lib/&lt;concern&gt;.cjs (fs-utils.cjs, diff.cjs, tarball.cjs — one file per util concern), (7) DISPATCH ladder in bin/install.js stays but shrinks to &lt;200 lines; only wires modules, doesn&apos;t implement, (8) per-runtime tests in tests/install/&lt;runtime&gt;.test.cjs — isolated from other runtimes&apos; changes, (9) per-converter tests in tests/install/converters/&lt;type&gt;.test.cjs. Rule: if a function touches more than one runtime OR more than one converter concern, split it. Parallel-agent safety check: two agents editing install/runtimes/claude.cjs and install/runtimes/cursor.cjs simultaneously should never conflict. Not a prerequisite for 63-28 but strongly recommended BEFORE 63-28 lands manifest-emit logic — adding new modes to the 5,687-line monolith guarantees toe-stepping.
63-45 planned SUPERSEDED / REDUNDANT — command &apos;gad tasks update&apos; already exists at vendor/get-anything-done/bin/commands/tasks/update.cjs (supports --goal, --print, dual-writes XML + per-task JSON). Operator flagged this 2026-04-22: I failed to check for the command&apos;s existence before registering the task. What IS missing (keep as a real sub-gap): --type / --status / --depends / --append-goal flags on update (today only --goal works). File: possibly rewrite this task narrowly to &apos;extend gad tasks update with --type/--status/--depends/--append-goal args&apos;. Status: close or rewrite per operator.
63-46 planned Clean review + documentation of install/update mechanisms across four distinct surfaces: (1) GAD CLI binary gad.exe, (2) GAD TUI binary gad-tui.exe, (3) Desktop app apps/desktop (Tauri bundle), (4) Framework distribution (skills/workflows/hooks into consumer runtimes via bin/install.js). Operator 2026-04-22: &apos;you must review all the install and update for gad cli/tui, desktop, the framework itself I guess, you are too confused.&apos; Subagent dispatched to produce .planning/notes/2026-04-22-install-update-review.md covering per-surface: what it is, source location, build command, build artifact, install mechanism, install failure modes, update mechanism, update failure modes, open tasks affecting it, open decisions affecting it, overlaps + confusion risks. Plus synthesis: where overlaps are real (e.g. TUI ships in gad release), where separations are clean, where state is inconsistent (stale docs referencing SEA post-gad-278 bun migration), recommended cleanup order. Not an execution task — it&apos;s the source-of-truth map that unblocks 63-28 (plugin manifests), 63-43 (GitHub Releases pipeline), 63-44 (install.js refactor), 63-23/26/27 (TUI bugs), 05-12/13/14 (desktop bugs) without continuing to conflate them.
63-47 planned Delete stale SEA + npx references across the repo — formal cleanup after gad-279 superseded gad-129 and gad-278 superseded gad-138. Explicit grep pre-flight: (1) grep -rn &apos;postject\|experimental-sea\|Node SEA\|SEA\b&apos; in bin/ scripts/ skills/ workflows/ references/ — list every hit, then remove or reword (SEA is retired, not &apos;deprecated escape hatch&apos;; build-bun-release.mjs writes this language into INSTALL.txt — fix the writer + any stale INSTALL.txt already shipped), (2) grep -rn &apos;npx get-anything-done\|npm install get-anything-done\|npm view get-anything-done\|npx get-shit-done\|get-shit-done-cc&apos; — remove or replace with &apos;gh release download&apos; per gad-188, (3) grep CLAUDE.md files for these patterns too, (4) vendor/get-anything-done/skills/gad-update/, skills/gad-reapply-patches/, skills/build-and-release-locally/ — known to have stale references, (5) check dist/release/INSTALL.txt if any shipped builds carry stale language. Single commit, no functional changes — doc hygiene only. Also verify: no remaining decisions cross-reference gad-129 or gad-138 as live direction; add superseded-by markers if missed.
63-48 planned Wire gad-tui.exe into .github/workflows/release-binaries.yml CI pipeline. Today scripts/build-bun-release.mjs (local) inline-invokes &apos;pnpm --filter @magicborn/gad-tui build:bun&apos; but CI release workflow does not. Result: tag-push releases ship gad.exe without gad-tui.exe; users who install via gh release download get a broken &apos;gad tui&apos; command. Action: (1) read current .github/workflows/release-binaries.yml to understand matrix + build steps, (2) add gad-tui build step (pnpm --filter @magicborn/gad-tui build:bun with correct target per matrix platform), (3) upload gad-tui-v&lt;ver&gt;-&lt;platform&gt;.exe as release asset alongside gad-v&lt;ver&gt;-&lt;platform&gt;.exe, (4) test via workflow_dispatch with a real tag before relying on it, (5) update CLI&apos;s gad self install to reference the new artifact naming if needed. Fixes gap identified in install/update review 2026-04-22.
63-49 planned Collapse gad (CLI+TUI as one surface) install paths to ONE canonical — no fallback. Operator 2026-04-22: &apos;we should never get confused about our install paths and how our users are and what paths should be needed.&apos; Today three compete: (a) install-gad-windows.ps1 -Artifact &lt;path-to-gad-vX.exe&gt;, (b) &apos;gad install self [--dir &lt;path&gt;]&apos; from packaged binary, (c) &apos;gad self build &amp;&amp; gad self install&apos; local-dogfood. Different cwd/deps/failure shapes + no Mac/Linux. Action: (1) pick ONE canonical path — propose install-gad-windows.ps1 for Windows + install-gad-unix.sh for macOS/Linux (both downloaded from GitHub Release alongside the binary), (2) retire the other two paths — &apos;gad install self&apos; becomes internal-only (used by the PS1 script after download), &apos;gad self install&apos; deleted or relabeled as dev-only with clear doc string, (3) install script ALWAYS installs both gad.exe and gad-tui.exe together (per 63-50 — they are one product), errors if either is missing from the downloaded artifact, (4) single canonical doc in CLAUDE.md: &apos;Install GAD: download from GitHub Release, run install-gad-&lt;platform&gt;.&lt;ext&gt;.&apos; One path, one command, one install location. Deletes 2 code paths.
63-50 planned Unify gad.exe + gad-tui.exe into a SINGLE binary with argv-based mode dispatch. Today the install dir at %LOCALAPPDATA%\Programs\gad\bin\ holds TWO separately-compiled bun binaries at ~115MB each (~230MB total, down to ~115MB after 2026-04-22 dupe deletion). Industry convention (claude, codex, gemini, opencode, free-code — all bun+TSX+React+Ink+single-binary-argv-dispatch) is ONE binary where &apos;gad&apos; (no args) enters interactive/TUI mode and &apos;gad &lt;subcommand&gt;&apos; runs non-interactive. PROVEN pattern — tmp/free-code/package.json bin &apos;claude&apos; -&gt; ./cli, entrypoint src/entrypoints/cli.tsx, bun build --compile via scripts/build.ts. Same tech stack as GAD. No spike needed. Action: (1) move packages/gad-tui/src/entry.tsx export to a default async function runTui(argv) that boots the Ink app, keep as workspace package for dev UX, (2) rewrite bin/commands/tui.cjs to require(&apos;@magicborn/gad-tui&apos;) + invoke runTui(rawArgs) — no spawnSync, no sibling .exe, (3) bin/gad.cjs: if no subcommand, route to &apos;tui&apos; subcommand — matches claude/codex convention where bare invocation = interactive, (4) scripts/build-bun-release.mjs drops the separate pnpm --filter @magicborn/gad-tui build:bun call + drops the TUI artifact copy step; single bun build --compile --target=bun-&lt;platform&gt; bin/gad.cjs produces the complete binary, (5) install script produces ONE gad.exe (per 63-49), (6) bin/commands/tui.cjs &apos;gad-tui is not installed&apos; error path deleted — impossible state, (7) legacy backward-compat: if require(&apos;@magicborn/gad-tui&apos;) throws, check for sibling gad-tui.exe and spawn (current behavior) for N releases, then drop. Effect: ~115MB saved per install, eliminates 2-artifact install problem, matches industry convention, supersedes 63-48 (TUI-in-CI becomes unnecessary once there&apos;s no separate TUI artifact).
63-51 planned Rename /gad-update → /gad-framework-update AND add new /gad-cli-update. Current /gad-update only updates Surface 4 (skills/workflows/hooks in consumer runtime config dirs) but the name implies it updates Surface 1 (gad.exe binary on PATH). Confusion called out in install/update review 2026-04-22. Action: (1) rename skill dir vendor/get-anything-done/skills/gad-update/ → skills/gad-framework-update/ + workflow file, update frontmatter name, (2) author new skill skills/gad-cli-update/ that fetches newer gad.exe via gh release download + runs &apos;gad install self&apos; or &apos;install-gad-windows.ps1 -Artifact &lt;path&gt;&apos;, (3) add a &apos;gad update&apos; CLI dispatcher subcommand (not a skill, a real CLI verb in bin/commands/) that routes to either framework or cli update based on arg (&apos;gad update framework&apos; vs &apos;gad update cli&apos; vs &apos;gad update all&apos;), (4) update SessionStart hook gad-check-update.js to check both surfaces independently + report separately, (5) update CLAUDE.md + any cross-refs in other skills, (6) handle legacy /gad-update alias for backwards compat during transition period.
63-52 planned Unify release-tag convention between vendor repo (v*) and monorepo (desktop-v*). Today CLI + framework release on vendor repo v* tags triggering .github/workflows/release-binaries.yml; desktop releases on monorepo desktop-v* tags triggering .github/workflows/desktop-build.yml. Neither auto-triggers the other. Operator must push both tags for a &apos;complete&apos; release spanning both surfaces. Pick one: (a) cross-repo dispatch — vendor&apos;s v* tag fires a repository_dispatch to monorepo which kicks off desktop-build — single push suffices, (b) documented dual-push convention — each release needs both tags, operator explicitly cuts both, naming consistent (&apos;v1.36.0&apos; in vendor = &apos;desktop-v1.36.0&apos; in monorepo = same release), (c) version pinning — desktop embeds a gad-cli-version in tauri.conf.json, updating it requires a new desktop-v*. Action: (1) operator decide among a/b/c, (2) implement chosen path (cross-repo dispatch if (a), doc-only if (b), version-pin logic if (c)), (3) document the convention in root CLAUDE.md + vendor CLAUDE.md, (4) update release scripts to match. Blocks: clean release story for end users who want &apos;latest GAD.&apos;
63-53 planned Retire TASK-REGISTRY.xml as a source of truth; per-task JSON under .planning/tasks/&lt;id&gt;.json becomes the sole canonical. Operator 2026-04-22: &apos;i dont want to maintain 2 sources of tasks, so just single source of truths.&apos; Today per decision 2026-04-20 D3 the reader precedence is per-task JSON → XML → legacy STATE.md, with dual-write on all gad tasks add/update/stamp/promote calls. The XML is de facto legacy already. Action: (1) audit the codebase for any code that READS from TASK-REGISTRY.xml — reader&apos;s XML fallback, any subagent-prompt snippets, any skill workflow that greps the XML, (2) for each reader: either migrate to tasks-dir reader OR accept it as a derived-read (regen XML from JSON on-demand), (3) stop dual-writing XML: gad tasks add/update/stamp write ONLY to per-task JSON, (4) delete TASK-REGISTRY.xml files across all projects (global, get-anything-done, llm-from-scratch, etc.) once no reader needs them, (5) update vendor/get-anything-done/CLAUDE.md: remove &apos;TASK-REGISTRY.xml — single 2k-line file, edit one task block at a time&apos; (stale guidance), (6) gad tasks migrate command stays but becomes one-way import for foreign projects inheriting XML from an older GAD version. Coverage check: 547 XML entries + 519 JSON files in vendor — JSON is 95% caught up but not 100%; run gad tasks migrate --projectid get-anything-done --force before retiring XML to catch the delta.
63-54 planned Update &apos;gad graph build&apos; help text to reflect post-D3 data sources. Today help says &apos;Generate graph.json + graph.html from .planning/ XML files&apos; but the underlying task-registry-reader.cjs prefers per-task JSON under .planning/tasks/&lt;id&gt;.json since decision 2026-04-20 D3 (task 63-11). Help text is stale. Action: (1) update the help string in bin/commands/graph.cjs (or wherever it&apos;s defined) to say &apos;from .planning/ files (per-task JSON + XML fallback + decisions + roadmap + state)&apos;, (2) grep other command help texts for stale &apos;XML&apos; / &apos;XML files&apos; references — phases, tasks, decisions, etc. likely have similar staleness, (3) add a line to vendor/get-anything-done/CLAUDE.md under data sources: &apos;Per-task JSON is canonical (D3). TASK-REGISTRY.xml is legacy fallback, slated for retirement per 63-53.&apos; Small but removes operator-facing confusion about what sources are current.
```

## What this candidate is for

This file was auto-generated by `compute-self-eval.mjs` because phase
63 exceeded the selection pressure threshold. High pressure
signals a recurring pattern that may want to become a reusable skill.

This is the **raw input** to the drafting step. The drafter (`create-proto-skill`,
invoked by `gad-evolution-evolve`) reads this file and decides what the
skill should be. No curator pre-digestion happens — see the 2026-04-13
evolution-loop experiment finding for why
(`evals/FINDINGS-2026-04-13-evolution-loop-experiment.md`).

## How the drafter should enrich this

The drafter should pull additional context from:

- `gad decisions --projectid get-anything-done | grep -i <keyword>` —
  relevant decisions for this phase
- `git log --follow --oneline <file>` — historical context for files this
  phase touched (catches the "three attempts at task X failed" thread that
  lives in commit history)
- `gad --help` and `gad <subcommand> --help` — CLI surface available
  to the skill
- `ls vendor/get-anything-done/skills/` — existing skills to avoid
  duplicating

The drafter does **not** ask a human for anything. Make decisions and
document them in the SKILL.md.

## Output location

The drafter writes to `.planning/proto-skills/phase-63-agent-state-session-hygiene/SKILL.md` — a
**different directory** from this candidate. Candidates and proto-skills
are two distinct stages:

- **candidate** (this file) = raw selection-pressure output
- **proto-skill** (drafter output) = drafted SKILL.md awaiting human review
- **skill** (post-promote) = lives in `skills/` as part of species DNA

The human reviewer runs `gad evolution promote <slug>` or
`gad evolution discard <slug>` after reviewing the proto-skill.
