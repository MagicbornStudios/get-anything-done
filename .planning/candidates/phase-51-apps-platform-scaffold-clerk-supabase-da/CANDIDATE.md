---
status: candidate
source_phase: "51"
source_phase_title: "apps/platform scaffold — Clerk + Supabase + data model"
pressure_score: 47.67
tasks_total: 9
tasks_done: 9
crosscuts: 7
created_on: "2026-04-23"
created_by: compute-self-eval
---

# Candidate from phase 51

## Phase

```
get-anything-done | 51 | apps/platform scaffold — Clerk + Supabase + data model
selection pressure: 47.67  (9 tasks, 9 done, 7 crosscuts)
```

## Tasks

```
51-01 done Clerk skeleton in apps/planning-app: install @clerk/nextjs, mount middleware that protects /api/dev/* and /projects/edit/*, render UserButton in header, expose helper that resolves operator id (Clerk userId or &apos;solo-operator&apos; fallback). Stamp publishedBy on the publish PATCH route from auth(). Solo-operator fallback when CLERK_PUBLISHABLE_KEY/CLERK_SECRET_KEY are unset so local dev keeps working without Clerk creds.
51-02 done Project Editor agent workspace at /project-editor + /project-editor/[project] in apps/planning-app: index lists registered projects with latest published generation chip, detail page renders ProjectTree (file browser via /api/dev/tree), forge-view iframe (latest preserved build via /api/dev/forge-view + /api/dev/forge-asset), and SSE-streamed agent launcher (/api/dev/spawn) with confirm dialog for claude-code/codex/gad-eval-run. Cross-links to /projects/edit/[id] (bestiary/BYOK) + /my-projects drawer. Middleware extends Clerk protection to /project-editor/* since spawn writes to disk.
51-03 done Subagent attribution: spawn route resolves operator via getOperatorId() and injects OPERATOR_ID + GAD_OPERATOR_ID + GAD_AGENT_ID + GAD_AGENT_ROLE + GAD_PROJECT_ID into child env, plus /api/dev/whoami returns the same payload for client-side surfaces. Subagent CLIs (claude-code, codex, gad eval run) can echo identity via env, and a callback into /api/dev/publish from inside a spawned child can pass x-operator-id header so MANIFEST.json publishedBy carries the human operator even when the actual write came from an agent runtime.
51-04 done /my-published view in apps/planning-app: server reads marketplace-index.generated.json and filters generations where publishedBy === getOperatorId(). Solo-operator mode shows everything publishedBy &apos;solo-operator&apos;; Clerk mode shows the signed-in user&apos;s generations only. Each row links to /projects/edit/[id] (revert/manage) and to the public playable URL. Empty state explains how to publish.
51-05 done Hot-rebuild marketplace-index.generated.json on publish so /my-published and the public marketplace surface state changes without a full vendor-site predev. Avoid re-scanning every TRACE.json — mutate the existing index in place, preserving fields the publish PATCH can&apos;t recompute (score, date, contextFramework).
51-06 done Per-project best/latest score chips on /project-market/species/[species] detail page. Operators evaluating a species&apos;s portfolio against one project want to see the high-water mark and the most recent scored attempt at a glance, not just the count of generations.
51-07 done gad publish CLI command with operator attribution. Subagents spawned via /api/dev/spawn inherit OPERATOR_ID/GAD_AGENT_ID env vars (task 51-03) but had no CLI consumer that actually threaded them as request headers. Provide one so an agent runtime calling `gad publish escape-the-dungeon --species gad --version v7` from inside a spawned shell stamps MANIFEST.publishedBy with the human operator, not the runtime.
51-08 done gad publish list — read-only inspection of the marketplace index from the CLI. Operators want to confirm what their identity has actually shipped without opening /my-published in a browser, and agents want a way to assert published state in scripts without spinning up planning-app.
51-09 done UI publish actions need to be distinguishable from CLI/agent publishes in the audit trail. Without a marker, server logs show `operator=&lt;id&gt;` for both a Bestiary tab click and a `gad publish` call from an agent, hiding intent.
```

## What this candidate is for

This file was auto-generated by `compute-self-eval.mjs` because phase
51 exceeded the selection pressure threshold. High pressure
signals a recurring pattern that may want to become a reusable skill.

This is the **raw input** to the drafting step. The drafter (`create-proto-skill`,
invoked by `gad-evolution-evolve`) reads this file and decides what the
skill should be. No curator pre-digestion happens — see the 2026-04-13
evolution-loop experiment finding for why
(`evals/FINDINGS-2026-04-13-evolution-loop-experiment.md`).

## How the drafter should enrich this

The drafter should pull additional context from:

- `gad decisions --projectid get-anything-done | grep -i <keyword>` —
  relevant decisions for this phase
- `git log --follow --oneline <file>` — historical context for files this
  phase touched (catches the "three attempts at task X failed" thread that
  lives in commit history)
- `gad --help` and `gad <subcommand> --help` — CLI surface available
  to the skill
- `ls vendor/get-anything-done/skills/` — existing skills to avoid
  duplicating

The drafter does **not** ask a human for anything. Make decisions and
document them in the SKILL.md.

## Output location

The drafter writes to `.planning/proto-skills/phase-51-apps-platform-scaffold-clerk-supabase-da/SKILL.md` — a
**different directory** from this candidate. Candidates and proto-skills
are two distinct stages:

- **candidate** (this file) = raw selection-pressure output
- **proto-skill** (drafter output) = drafted SKILL.md awaiting human review
- **skill** (post-promote) = lives in `skills/` as part of species DNA

The human reviewer runs `gad evolution promote <slug>` or
`gad evolution discard <slug>` after reviewing the proto-skill.
