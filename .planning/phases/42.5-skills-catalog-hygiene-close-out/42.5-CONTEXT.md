# Phase 42.5 — Skills catalog hygiene close-out

## Summary

Skills cleanup COMPLETE via 3 parallel subagents. Canonical vendor skills
92 → 73 (−19, 21% reduction). Deleted 16 COLLAPSE+STALE skills + 3 folded
into canonical targets + 1 vestigial. Renamed `gad-eval-spawn` →
`gad-generation-spawn` with full vocabulary refresh. Refreshed
`objective-eval-design`, `self-eval`, `eval-skill-install` per decision
gad-212 (species/generation). Build-site-data.mjs parseContextFrameworks
parser removed. Skill-sync re-run clean. Graph rebuilt. Cursor allowlist:
43 auto-loaded (under 63 budget). Claude visibility=open (all 73
invocable after session restart). Reports at
`.planning/notes/2026-04-22-skill-{deletion,fold,vocabulary-refresh}-report.md`
+ `2026-04-22-skills-audit{,-reverify}.md`. (Source: state-log
2026-04-22T23:46:35.576Z, with claude-visibility figure updated post
decision gad-283.)

## Catalog size before/after

| Dir | Before | After |
|---|---|---|
| `vendor/get-anything-done/skills/` (canonical) | 92 | 73 (+ `emergent/` = 74 dirs) |
| `.claude/skills/` | 93 | 74 (visibility=open, all unflagged) |
| `.cursor/skills/` | 95 | 77 (gated; 43 unflagged ≤ 63 budget) |
| `.codex/skills/` | 115 | 96 (visibility=closed) |
| `.agents/skills/` | 7 | 7 (unchanged; not in skill_sync.runtimes) |

## Removed — 19 canonical slugs

### Deleted outright (16, task 63-55)

- gad-add-backlog, gad-add-phase, gad-insert-phase, gad-remove-phase
- gad-set-profile, gad-stats, gad-help
- gad-eval-bootstrap, gad-eval-list, gad-eval-report, gad-eval-run,
  gad-eval-suite
- gad-session
- gad-pause-work (logic folded into gad-handoffs — task 63-39)
- gad-resume-work (logic folded into gad-handoffs — task 63-39)
- phase-42.4-context-frameworks (operator directive — no separate context
  frameworks)

### Folded into existing skills (3)

| Retired | Folded into | Task |
|---|---|---|
| gad-cleanup | gad-complete-milestone (archive_milestone step) | 63-35 |
| gad-auto-conventions | gad-map-codebase (`--focus=conventions` scope) | 63-41 |
| gad-migrate-schema | (vestigial — no target; XML→MD direction obsolete) | 63-36 |

## Renamed (1)

| Before | After | Task |
|---|---|---|
| gad-eval-spawn | gad-generation-spawn | 63-38 |

Workflow renamed `workflows/eval-spawn.md` → `workflows/generation-spawn.md`.
Env vars `GAD_EVAL_*` → `GAD_GENERATION_*` / `GAD_SPECIES` (documented in
SKILL.md; CLI-side env-var rename tracked separately under gad-212).
Session-marker dir `.planning/.eval-runs/` → `.planning/.generation-runs/`
(.gitignore updated, legacy path kept for transition).

## Vocabulary-refreshed (3, task 63-40)

| Skill | Substance |
|---|---|
| objective-eval-design | ~11 `eval→species/generation` substitutions; methodology language preserved; appendix added |
| self-eval | "Evals run" → "Generations spawned"; 6-dim rubric preserved verbatim |
| eval-skill-install | `gad eval *` → `gad species setup` + `gad spawn` + `gad generation preserve`; transition-alias note |

Slugs intentionally preserved — each names an evaluation methodology, not
an artifact (per task 63-40 constraint "preserve 'evaluation' where
academic methodology term").

## Residual references found + fixed in Task 1

2 active references fixed this pass; 1 historical-note reference left
intact:

| File | Line | Before | After |
|---|---|---|---|
| `vendor/get-anything-done/skills/gad-handoffs/SKILL.md` | 250 | "Replaces the old `gad-pause-work` skill. Whenever you need to stop…" | "Whenever you need to stop…" |
| `vendor/get-anything-done/references/skill-shape.md` | 259 | legacy-prefix list included `gad-eval-run.md` | removed (list now: gad-debug, gad-execute-phase, gad-map-codebase, gad-new-project, gad-plan-phase) |

Kept (historical/note context):

- `references/skill-types.md:33` — sentence explicitly documents the
  2026-04-22 fold ("Previously `gad-auto-conventions` also sat here;
  folded into `gad-map-codebase` 2026-04-22, task 63-41."). Intentional
  audit-trail reference; retained.
- `references/project-shape.md` — no remaining deleted-slug references
  (fold subagent cleared them).

Orphan skill dir removed from `.codex/skills/gad-migrate-schema/` (pre-existing
orphan; deleted manually before re-sync).

## Task reassignment (Task 4)

10 tasks moved from phase 63 to phase 42.5:

| Task | Status | Note |
|---|---|---|
| 63-29 | done | skills audit (stamped this pass) |
| 63-33 | done | re-verify COLLAPSE verdicts (stamped this pass) |
| 63-34 | done | superpowers deep review (stamped this pass) |
| 63-35 | done | fold gad-cleanup |
| 63-36 | done | delete gad-migrate-schema |
| 63-37 | planned | gad-milestone umbrella keep-or-collapse audit (carried forward) |
| 63-38 | done | rename gad-eval-spawn → gad-generation-spawn |
| 63-39 | done | fold pause/resume-work → handoffs |
| 63-40 | done | vocabulary refresh |
| 63-41 | done | delete gad-auto-conventions |

Reassignment limitation: task IDs start with `63-` and
`task-registry-reader.cjs:122` derives `phase` from
`id.split('-')[0]`. `.planning/tasks/*.json` was hand-patched to
`"phase": "42.5"` (survives `gad tasks stamp` only if reader is fixed or
on read via JSON-first path). XML `<phase id="42.5">` now contains the 10
task blocks — the grouping is correct in the source of truth.

## Canonical assets validation (Task 3)

`node vendor/get-anything-done/scripts/validate-canonical-assets.mjs` →
`Canonical asset validation passed.`

## Open items

- **63-37** (gad-milestone umbrella audit) — remains status=planned. Carry
  forward to a future phase (42.6 or later) or close with "keep as
  router" decision. Not blocking phase 42.5 completion.
- **Task-registry ID-derived phase** — reader derives phase from task ID
  prefix. True reassignment of phase for historical 63-NN tasks requires
  either (a) renaming task IDs (breaking audit trail) or (b) patching the
  reader to prefer the JSON-declared phase. Deferred.
- **XML-vs-JSON status drift** — `gad tasks stamp` updated
  `.planning/tasks/<id>.json` to `status=done` for 9 tasks; the XML
  `TASK-REGISTRY.xml` still shows `status="planned"` for those rows (the
  CLI canonicalizes via JSON-first read). Not a correctness issue, but
  worth a one-pass XML status sync in a later cleanup.
- **Generated site artifacts** — `site/lib/catalog.generated.ts` +
  `site/lib/eval-data.generated.js` regenerate clean on next
  `npm run prebuild`. No action needed this phase.

## Success criteria — met

- Canonical skill count 73 ✓
- Cursor allowlist ≤ 63 auto-loaded (43 / 63) ✓
- Claude visibility=open (74 unflagged) ✓
- Zero residual references to deleted slugs in active reference files ✓
- validate-canonical-assets passes ✓
- Decision gad-283 (gad-config.toml sole config surface) in place ✓

## Success criteria — unmet / carried

- 63-37 gad-milestone umbrella verdict — carry forward.
- XML status sync for 63-35/36/38/39/40/41 rows — carry forward.
