<?xml version="1.0" encoding="UTF-8"?>
<errors-and-attempts>
  <entry id="dev-panel-split-regression-2026-04-14" date="2026-04-14">
    <summary>Refactoring the dev panel into divergent section-vs-band surfaces regressed the operator workflow: the compact panel kept good ids, while the long section panel drifted away from the sticky compact UX and source-searchable copy tokens.</summary>
    <context>
      During public-site dev-id work on 2026-04-14, the shared panel logic was split into a compact band panel (used by nav/footer and later page bands) and a longer section scan panel. The compact panel was closer to the desired operator workflow, but the section panel kept a different layout and copied less useful tokens in some paths. At the same time, some section shells were still relying on auto-generated runtime ids, which made copied values hard to grep in source.
    </context>
    <failure>
      Two mistakes combined:
      (1) treated "section panel UX" and "band panel UX" as separate products instead of one operator surface with different scopes;
      (2) let runtime-generated ids remain in the path where the operator actually copies identifiers for Codex/Cursor/Claude handoff.
      The result was inconsistency exactly where the panel needs to be most deterministic.
    </failure>
    <rule>
      The dev panel is one product. Do not maintain separate compact-vs-long semantics for the same actions. Any panel row or prompt handoff must prefer a source-searchable literal (`cid`, `stableCid`, `stableBandCid`, or `as`) over a runtime-only generated id. If a refactor improves one panel mode but not the other, stop and unify the surface before extending it further.
    </rule>
    <reference>vendor/get-anything-done/site/components/devid/DevPanel.tsx</reference>
    <reference>vendor/get-anything-done/site/components/devid/Identified.tsx</reference>
    <reference>vendor/get-anything-done/site/components/devid/PageIdentified.tsx</reference>
    <reference>vendor/get-anything-done/site/components/site/SiteSection.tsx</reference>
    <reference>vendor/get-anything-done/.planning/TASK-REGISTRY.xml (44-04, 44-05)</reference>
  </entry>
  <entry id="forge-init-md-misaction-2026-04-14" date="2026-04-14">
    <summary>Hand-converted markdown planning files to XML instead of using a CLI command — the CLI command does not exist, but the gap should have been surfaced before doing the conversion.</summary>
    <context>
      During app-forge project scaffolding on 2026-04-14, ran `gad projects init --name app-forge --path apps/forge`. The CLI emitted four markdown template files (PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md) containing literal placeholder content from the new-project skill template. The rest of the monorepo uses 6+ canonical XML files (STATE.xml, ROADMAP.xml, TASK-REGISTRY.xml, DECISIONS.xml, ERRORS-AND-ATTEMPTS.xml, REQUIREMENTS.xml). After being asked to convert to XML, I hand-wrote the XML files instead of using a CLI command.
    </context>
    <failure>
      Two compounding errors:
      (1) Used `gad projects init` without first checking whether its output matched the repo's canonical format. The misalignment between "what init scaffolds" and "what every other planning root looks like" should have been a stop-and-flag moment before any files were written.
      (2) When asked to convert, hand-rolled the XML instead of looking for or building a `gad migrate-schema --from md --to xml` (or equivalent) command. `gad migrate-schema --help` only shows RP→GAD-Markdown conversion. The right move was to surface the gap as a framework task in this file and the task registry, then ask the user whether to add the CLI command first or hand-convert as a one-off — not silently hand-roll.
    </failure>
    <rule>
      Before doing any structural change to planning files (format conversion, schema migration, file-set normalization), look for a `gad &lt;verb&gt;` command that does it. If none exists, STOP and surface the gap as a task against `get-anything-done` and an entry in this file. Then ask the user: "no CLI command for this — should I (a) add one to GAD first, or (b) hand-convert as a one-off?" Never silently hand-roll structural changes.

      Also: after running `gad projects init` for a new planning root, immediately verify the output matches the canonical file set used elsewhere in the repo. Drift between "what init scaffolds" and "what reality looks like" is a framework gap that must be filed against `get-anything-done`, not papered over.
    </rule>
    <reference>apps/forge/.planning/</reference>
    <reference>vendor/get-anything-done/.planning/TASK-REGISTRY.xml (tasks 42.4-08, 42.4-09, 42.4-10)</reference>
    <reference>C:\Users\benja\.claude\projects\C--Users-benja-Documents-custom-portfolio\memory\feedback_use_cli_for_conversions.md</reference>
    <reference>C:\Users\benja\.claude\projects\C--Users-benja-Documents-custom-portfolio\memory\feedback_human_todos_scope.md</reference>
  </entry>
  <entry id="mermaid-leak-footer-push-2026-04-14" date="2026-04-14">
    <summary>Mermaid syntax errors were rendered as leaked body text below page content, visually pushing footer context and confusing landmark-driven editing on /gad.</summary>
    <context>
      During speech-driven UPDATE testing against `/gad`, the operator reported text like "Mermaid JS syntax error ... mermaid version 11.14.0" appearing below the page and interfering with footer-area reasoning. The target landmark was `site-footer`, but root cause was not Footer itself.
    </context>
    <failure>
      Mermaid render failures in workflow diagram components could leave temporary error nodes in `document.body`, outside component hosts. Those leaked nodes persisted across navigation and looked like footer/layout regressions on unrelated routes.
    </failure>
    <rule>
      For visual-landmark debugging, resolve by landmark first but always validate cross-route side effects from shared client renderers. Mermaid/graph renderers must clean up temporary DOM artifacts after each render attempt, and leaked error nodes must be removed unless inside explicit diagram hosts.
    </rule>
    <reference>vendor/get-anything-done/site/components/landing/workflow/WorkflowMermaidDiagram.tsx</reference>
    <reference>vendor/get-anything-done/site/components/workflow/WorkflowMermaidDiagram.tsx</reference>
    <reference>vendor/get-anything-done/references/visual-context-panel-methodology.md</reference>
  </entry>

  <entry id="73-preexisting-model-override-test" date="2026-04-30" phase="73" task="73-01" status="open" kind="framework">
    <summary>gad-tools-models test: 'honors explicit model overrides' fails pre-existing — resolveAgentModel returns alias not full ID</summary>
    <context>Test at line 64 passes model_overrides={gad-planner: 'claude-sonnet-4-6'} and expects planner_model to be 'claude-sonnet-4-6', but resolveAgentModel returns 'sonnet' alias instead of full model ID string.</context>
    <failure>AssertionError: actual 'sonnet' expected 'claude-sonnet-4-6'. Pre-existed before phase 73 work.</failure>
    <rule>resolveAgentModel returns short aliases (sonnet, haiku, inherit) not full model IDs. Tests expecting full IDs must fix normalizeOverride to pass through unknown strings, or match alias behavior.</rule>
    <reference>tests/gad-tools-models.test.cjs:64</reference>
    <reference>lib/model-profiles.cjs:normalizeOverride</reference>
  </entry>
  <entry id="74-preexisting-commands-test-stats-checkcommit" date="2026-04-30" phase="74" task="74-01" status="open" kind="framework">
    <summary>tests/commands.test.cjs: 75 pre-existing failures — "Unknown command: stats" and "Unknown command: check-commit" in gad-tools calls</summary>
    <context>Verified before and after phase-74 changes by running node --test tests/commands.test.cjs. Both runs report exactly 75 failures, 0 passes. Failures involve Unknown command: stats and Unknown command: check-commit/progress subcommand json. Unrelated to friction-fix scope.</context>
    <failure>AssertionError: Command failed: Error: Unknown command: stats / check-commit. Pre-existed before phase 74.</failure>
    <rule>tests/commands.test.cjs requires gad-tools commands (stats, check-commit) that are not yet implemented. Do not treat as regression from CLI argv or decisions changes. Track against phase 73 (gad-tools verb gaps).</rule>
    <reference>tests/commands.test.cjs</reference>
    <reference>.planning/tasks/73-01.json</reference>
  </entry>

  <entry id="codex-runtime-launch-silent-fail-2026-04-30" date="2026-04-30" status="open" kind="framework">
    <summary>`gad runtime launch --force-runtime codex-cli --no-new-shell --same-shell` accepted contradictory flags, exited 0, wrote no output, and Codex never executed. Operator was AFK; orchestrator only noticed when checking output file (0 bytes).</summary>
    <context>
      Working on 7greens project (sites/7greens) bootstrap during Phase 01 execution on 2026-04-30. Dispatched Codex via:

        gad runtime launch \
          --projectid 7greens \
          --force-runtime codex-cli \
          --no-new-shell --same-shell \
          --launch-args "exec --full-auto --skip-git-repo-check '...prompt...'" \
          (run_in_background=true via Bash)

      `--no-new-shell` and `--same-shell` are documented as opposite intents. CLI accepted both, no warning. Background bash exited cleanly. Codex never wrote any files; output file was 0 bytes. The runtime harness produced empty .planning/sites/7greens/.planning/phases/01-foundation/ stub directories (likely from session-bootstrap side effects), then nothing. The orchestrator (Claude Opus 4.7) wasted ~5 min waiting before realizing the dispatch had silently failed and pivoting to direct file writes.
    </context>
    <failure>
      Three compounding issues:
      (1) `--no-new-shell` and `--same-shell` are mutually exclusive but the CLI does not validate or error when both are set — silently picks one (or neither) and proceeds.
      (2) When the chosen path fails (e.g. spawn refused, auth missing, codex-cli not found), the runtime harness exits 0 with no diagnostics in stderr or in `.planning/.gad-log/`. There is no signal upstream that "codex never ran."
      (3) For `--launch-args` containing a long quoted prompt, single-quoted strings interact poorly with bash arg parsing on Windows; the actual prompt that reaches `codex exec` may be truncated or mangled, but no echo of the resolved command is emitted unless `--dry-run` is used.
      The combination means a confidently-spawned Codex agent that "did nothing" looks identical (from the orchestrator's view) to a normal slow startup.
    </failure>
    <rule>
      `gad runtime launch` must (a) reject mutually exclusive flags with a non-zero exit and a clear error, and (b) write a "dispatch attempted" entry to `.planning/.gad-log/runtime-launch-*.jsonl` BEFORE the runtime is invoked, including the resolved command, so failures are observable. When `--launch-args` is a long string, the harness should echo the resolved argv to stderr at INFO level so orchestrators can confirm the prompt arrived intact. Until those land, orchestrators dispatching Codex via `gad runtime launch` should ALSO `tail -f .planning/.gad-log/` and confirm a corresponding session entry appears within 30s; absence == silent failure, switch to direct execution.
    </rule>
    <reference>bin/commands/runtime/launch.cjs (or wherever runtime launch is wired)</reference>
    <reference>.planning/.gad-log/ (no entry created during the failed dispatch)</reference>
    <reference>sites/7greens/.planning/phases/01-foundation/ (empty stub dirs left behind)</reference>
  </entry>
</errors-and-attempts>
