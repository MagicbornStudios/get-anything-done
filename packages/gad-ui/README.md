# gad-ui (planned)

Official **React** components for embedding GAD planning UIs in host apps (e.g. portfolio).

- **Owner:** `get-anything-done` monorepo / npm package.
- **Track:** `custom_portfolio` phase **06**, task **06-02** (`.planning/TASK-REGISTRY.xml`).
- **Replaces:** vendored `repo-planner` cockpit package in the portfolio.

This directory is a placeholder until the first components land.
