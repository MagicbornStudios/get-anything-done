# Platform app URL — used by vendor site links that point at platform surfaces
# (marketplace, post-auth pages, etc.)
# Default: http://localhost:3002 (platform dev server)
# Production: set to your deployed platform domain, e.g. https://platform.yoursite.com
NEXT_PUBLIC_PLATFORM_URL=http://localhost:3002
